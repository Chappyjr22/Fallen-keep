export const KING_ENTRANCE=[['The last ascent','The seals are broken. Above the stair, the keep falls silent.',0],['An audience earned','Beyond the doors, a thousand forgotten oaths whisper your name.',0],['The throne answers','“You broke my wards. You mistook them for my strength.”',1],['The final command','“Every soul in these halls still answers to me. Kneel, or join them.”',1]];
export const KING_DEFEAT=[['A crown unbound','“I am… the keep…” His voice breaks as emerald fire escapes the armor.',2],['The silence after','The crown strikes the marble. For the first time, the dead are silent. Beyond the throne, another road waits.',3]];
export const kingSceneArt=cell=>`<div class="comic-art king-finale-art" style="background-position:${cell%2*100}% ${Math.floor(cell/2)*100}%"></div>`;
export function playKingStory(root,pages,done){let n=0;const render=()=>{const p=pages[n];root.innerHTML=`<div class="panel king-story"><p class="eyebrow">${n+1} / ${pages.length} · ${p[0]}</p>${kingSceneArt(p[2])}<h2>${p[0]}</h2><p>${p[1]}</p><div class="actions"><button id="king-story-next" class="primary">${n===pages.length-1?'CONTINUE':'NEXT'}</button><button id="king-story-skip" class="secondary">Skip cinematic</button></div></div>`;root.querySelector('#king-story-next').onclick=()=>{if(n>=pages.length-1)done();else{n++;render();}};root.querySelector('#king-story-skip').onclick=done;};render();}
