import test from 'node:test';import assert from 'node:assert/strict';import {guestRequest} from '../public/guest.mjs';
test('guest coins persist locally, repeated saves are safe and purchases apply next run',()=>{globalThis.localStorage={value:null,getItem(){return this.value},setItem(k,v){this.value=v}};guestRequest('run/start',{runId:'one'});const body={runId:'one',gold:67,finished:true};guestRequest('run/checkpoint',body);guestRequest('run/checkpoint',body);assert.equal(guestRequest('profile').coins,67);guestRequest('purchase',{upgrade:'speed',rank:0});assert.equal(guestRequest('profile').coins,37);assert.equal(guestRequest('run/start',{runId:'two'}).bonuses.speed,1);assert.equal(guestRequest('run/start',{runId:'one'}).bonuses.speed,0);delete globalThis.localStorage;});
